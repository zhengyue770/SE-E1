项目描述：

	此包包括五个文件:
	linkTable.h ---------链表接口头文件
	linkTable.c ---------链表操作函数具体实现
	menu.h --------------菜单接口头文件
	menu.c --------------菜单操作具体视线
	menuTest.c ----------测试菜单接口调用

编译方法：
  
	gcc menu.c menuTest.c linkTable.c -o menuTest

使用指南：

	在menuTest.c中测试了各个菜单接口的调用。各接口的使用方法参照menuTest.c。
	先创建一个菜单，然后添加了四个命令，包括他们的名字、描述和次命令要 执行的操作。然后启动菜单程序。输入exit退出菜单后，删除了version命令以及与之相关的描述和执行操作之后，在此启动菜单查看菜单剩下的命令信息。输入exit退出菜单后，删除菜单。
